[project]
name = "google-batch-monitor"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "gentropy>=2.2.0",
    "google-cloud-batch>=0.4.1",
    "google-cloud-profiler>=4.1.0",
    "loguru>=0.7.3",
]
